# crm-django
CRM backend desarrollado con Django y PostgreSQL como proyecto formativo. Incluye autenticación, gestión de usuarios, roles, arquitectura modular y API REST preparada para integración con frontend (React).
